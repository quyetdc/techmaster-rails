<div class="col-md-12">
  <div class="pull-left"><h2> List Banners </h2></div>
  <div class="pull-right admin-new-btn">
    <%= link_to 'New Banner', new_admin_banner_path, class: 'btn btn-primary' %>
  </div>
</div>

<div class="col-md-12">
  <hr>
</div>

<div class="col-md-12">
  <table class="table table-hover">
    <thead>
        <th>Id</th>
        <th>Name</th>
        <th>Image</th>
        <th>Description</th>
        <th>Actions</th>
    </thead>

    <tbody>
      <% @banners.each do |banner| %>
        <tr>
          <td>
            <%= banner.id %>
          </td>

          <td>
            <%= banner.name %>
          </td>

          <td>
            <%= image_tag banner.image, width: '100px', height: 'auto' %>
          </td>

          <td>
            <%= banner.description %>
          </td>

          <td>
            <%= link_to 'View', admin_banner_path(banner), class: "btn btn-default" %>
            <%= link_to 'Edit', edit_admin_banner_path(banner), class: "btn btn-default" %>
            <%= link_to 'Destroy', admin_banner_path(banner), method: :delete, data: {confirm: 'Are you sure?'}, class: "btn btn-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
